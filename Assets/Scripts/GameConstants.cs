using System;
using System.Collections;

namespace GameConstants {
	public enum BUG_SIZE {SMALL=1, MEDIUM, LARGE};
	public enum BUG_DIFFICULTY {EASY, NORMAL, HARD};
	public enum BUG_DIRECTION {UP, LEFT, DOWN, RIGHT};
}